# AutoForge Terraform Variables — Environment-specific overrides

# ─── Staging Defaults ─────────────────────────────────
# terraform apply -var-file=staging.tfvars

project_id  = "autoforge-hackathon"
region      = "us-central1"
environment = "staging"
demo_mode   = true
